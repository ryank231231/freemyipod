This is a hacked version of the original extract2g utility to work with the
Nano 4G's new firmware changes. It breaks compatibility with all other firmware.

Here are the only changes made:

309c309
<       if ( (tmp = strtol(optarg, NULL, 0)) > 0 )
---
>       if ( (tmp = strtol(optarg, NULL, 0)) > -1 )
407c407
< 
---
> 		dir.devOffset += 4096;
